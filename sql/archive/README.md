# Archived SQL snippets

These files were split or duplicated the definitions already present in the repository. **Use the root [`database_full.sql`](../../database_full.sql) for new database installs** (core + bloodlines + caves + artifacts).

| File | Notes |
|------|--------|
| `database_schema.sql` | Remains at repo root as **core-only**; full install = `database_full.sql`. |
| Other files here | Legacy modular scripts kept for reference or historical migrations. |
